import { OutdateDirective } from './outdate.directive';

describe('OutdateDirective', () => {
  it('should create an instance', () => {
    const directive = new OutdateDirective();
    expect(directive).toBeTruthy();
  });
});
